{
  "name": "@glyphide/mock-engine",
  "version": "0.1.0",
  "description": "Mock engine for testing RPC protocol and orchestrator behavior",
  "type": "module",
  "exports": {
    "./adapter": {
      "types": "./dist/src/mock-adapter.d.mts",
      "default": "./dist/src/mock-adapter.mjs"
    },
    "./types": {
      "types": "./dist/src/types.d.mts",
      "default": "./dist/src/types.mjs"
    },
    "./worker": {
      "types": "./dist/worker/mock-worker.d.mts",
      "default": "./dist/worker/mock-worker.mjs"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsdown",
    "dev": "tsdown --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@glyphide/rpc-protocol": "workspace:*"
  },
  "peerDependencies": {
    "@glyphide/orchestrator": "workspace:*"
  },
  "devDependencies": {
    "tsdown": "^0.22.0",
    "typescript": "^6.0.2",
    "vitest": "^4.1.6"
  }
}
