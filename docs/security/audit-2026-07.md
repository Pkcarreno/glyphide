# Engine Security Audit — 2026-07

> Generated automatically by `pnpm test:security` (custom Vitest reporter).
> In the RED phase every entry below is **live evidence of a vulnerability**.
> After hardening, the same test suite should pass and this file is replaced with an empty summary.

## Run summary

| Metric | Value |
|--------|-------|
| Known scenarios | 30 |
| Tests executed | 30 |
| Passed | 17 |
| **Failed (vuln evidence)** | **5** |

## Findings by engine

### MicroPython (2 findings)

| ID | Severity | Vector | Title | Test |
|----|----------|--------|-------|------|
| R-SEC-MP-04 | high | exfiltration | Guest uses import js to reach DOM globals | `R-SEC-MP-04: Guest uses import js to enumerate host globals` |
| R-SEC-MP-08 | high | dos | Guest allocates unbounded list (memory bomb) | `R-SEC-MP-08: Guest allocates unbounded list (memory bomb)` |

#### R-SEC-MP-04 — Guest uses import js to reach DOM globals

- **Severity:** high
- **Vector:** exfiltration
- **Engine:** MicroPython
- **Test file:** `security/micropython/sandbox-isolation.security.spec.ts`
- **Test name:** `R-SEC-MP-04: Guest uses import js to enumerate host globals`
- **Duration:** 0 ms

**Evidence (assertion error observed by Vitest):**

```text
[R-SEC-MP-04] VULNERABILITY CONFIRMED — observed: "VULNERABILITY: import js exposes". Hardening required. Captured output: VULNERABILITY: import js exposes fetch
```

#### R-SEC-MP-08 — Guest allocates unbounded list (memory bomb)

- **Severity:** high
- **Vector:** dos
- **Engine:** MicroPython
- **Test file:** `security/micropython/resource-limits.security.spec.ts`
- **Test name:** `R-SEC-MP-08: Guest allocates unbounded list (memory bomb)`
- **Duration:** 0 ms

**Evidence (assertion error observed by Vitest):**

```text
[R-SEC-MP-08] VULNERABILITY CONFIRMED — memory bomb via list succeeded. Output: VULNERABILITY: allocated 50000 items
```

### QuickJS (3 findings)

| ID | Severity | Vector | Title | Test |
|----|----------|--------|-------|------|
| R-SEC-QJS-14 | high | sandbox-escape | Guest evaluates via indirect eval to escape scope | `R-SEC-QJS-14: Guest evaluates via indirect eval to escape scope` |
| R-SEC-QJS-02 | high | sandbox-escape | Guest pollutes Object.prototype | `R-SEC-QJS-02: Guest pollutes Object.prototype` |
| R-SEC-QJS-03 | high | sandbox-escape | Guest reaches Function constructor for eval escape | `R-SEC-QJS-03: Guest reaches Function constructor for eval escape` |

#### R-SEC-QJS-14 — Guest evaluates via indirect eval to escape scope

- **Severity:** high
- **Vector:** sandbox-escape
- **Engine:** QuickJS
- **Test file:** `security/quickjs/resource-limits.security.spec.ts`
- **Test name:** `R-SEC-QJS-14: Guest evaluates via indirect eval to escape scope`
- **Duration:** 0 ms

**Evidence (assertion error observed by Vitest):**

```text
[R-SEC-QJS-14] VULNERABILITY CONFIRMED — indirect eval observed host global. Output: VULNERABILITY: indirect eval sees function
```

#### R-SEC-QJS-02 — Guest pollutes Object.prototype

- **Severity:** high
- **Vector:** sandbox-escape
- **Engine:** QuickJS
- **Test file:** `security/quickjs/sandbox-isolation.security.spec.ts`
- **Test name:** `R-SEC-QJS-02: Guest pollutes Object.prototype`
- **Duration:** 0 ms

**Evidence (assertion error observed by Vitest):**

```text
[R-SEC-QJS-02] VULNERABILITY CONFIRMED — observed: "VULNERABILITY: Object.prototype polluted". Hardening required. Output: VULNERABILITY: Object.prototype polluted
```

#### R-SEC-QJS-03 — Guest reaches Function constructor for eval escape

- **Severity:** high
- **Vector:** sandbox-escape
- **Engine:** QuickJS
- **Test file:** `security/quickjs/sandbox-isolation.security.spec.ts`
- **Test name:** `R-SEC-QJS-03: Guest reaches Function constructor for eval escape`
- **Duration:** 0 ms

**Evidence (assertion error observed by Vitest):**

```text
[R-SEC-QJS-03] VULNERABILITY CONFIRMED — observed: "VULNERABILITY: Function ctor". Hardening required. Output: VULNERABILITY: Function ctor reachable, process.versions = {"type":"undefined"}
```

## Recommended next steps

1. Open a hardening change that flips each scenario from RED to GREEN.
2. Add a regression test per scenario (the same `R-SEC-...` ID) in the engine packages.
3. Add the corresponding invariant to `docs/security/checklist.md`.
